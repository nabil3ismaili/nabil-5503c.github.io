<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title id="pageTitle">Boutique</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --gold: #C9A96E;
            --gold-light: #E8D5A3;
            --black: #0a0a0a;
            --dark-gray: #1a1a1a;
            --medium-gray: #2a2a2a;
            --light-gray: #888;
            --white: #f5f5f0;
            --success: #4CAF50;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--black);
            color: var(--white);
            overflow-x: hidden;
        }

        .video-container {
            position: fixed; top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: -1; overflow: hidden;
        }
        .video-container video {
            min-width: 100%; min-height: 100%;
            object-fit: cover; opacity: 0.3;
        }
        .video-overlay {
            position: fixed; top: 0; left: 0;
            width: 100%; height: 100%;
            background: linear-gradient(to bottom, rgba(10,10,10,0.2), rgba(10,10,10,0.9));
            z-index: -1;
        }

        nav {
            position: fixed; top: 0; width: 100%;
            padding: 1.5rem 3rem;
            display: flex; justify-content: space-between; align-items: center;
            z-index: 1000;
            background: linear-gradient(to bottom, rgba(10,10,10,0.95), transparent);
        }
        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 1.6rem; font-weight: 700;
            letter-spacing: 4px; color: var(--gold);
        }
        .nav-right {
            display: flex; align-items: center; gap: 1.5rem;
        }
        .cart-btn {
            position: relative;
            background: none; border: 1px solid var(--gold);
            color: var(--gold); padding: 0.6rem 1.2rem;
            font-family: 'Inter', sans-serif; font-size: 0.8rem;
            letter-spacing: 2px; text-transform: uppercase;
            cursor: pointer; transition: all 0.3s;
        }
        .cart-btn:hover { background: var(--gold); color: var(--black); }
        .cart-count {
            position: absolute; top: -8px; right: -8px;
            background: var(--gold); color: var(--black);
            width: 20px; height: 20px; border-radius: 50%;
            font-size: 0.7rem; font-weight: 600;
            display: flex; align-items: center; justify-content: center;
        }

        .hero {
            height: 100vh; display: flex; flex-direction: column;
            justify-content: center; align-items: center;
            text-align: center; padding: 0 2rem; position: relative;
        }
        .hero-subtitle {
            font-size: 0.85rem; letter-spacing: 6px;
            text-transform: uppercase; color: var(--gold);
            margin-bottom: 2rem;
        }
        .hero-title {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2.5rem, 7vw, 5rem);
            font-weight: 400; line-height: 1.1; margin-bottom: 2rem;
        }
        .hero-title em { font-style: italic; color: var(--gold-light); }
        .hero-desc {
            font-size: 1rem; color: var(--light-gray);
            max-width: 550px; line-height: 1.8; margin-bottom: 3rem;
        }
        .cta-button {
            padding: 1.2rem 3rem; background: transparent;
            border: 1px solid var(--gold); color: var(--gold);
            font-family: 'Inter', sans-serif; font-size: 0.85rem;
            letter-spacing: 3px; text-transform: uppercase;
            cursor: pointer; transition: all 0.4s; text-decoration: none;
        }
        .cta-button:hover { background: var(--gold); color: var(--black); }

        .products-section {
            padding: 6rem 3rem;
            background: linear-gradient(to bottom, transparent, var(--black) 15%);
        }
        .section-header { text-align: center; margin-bottom: 4rem; }
        .section-label {
            font-size: 0.75rem; letter-spacing: 4px;
            text-transform: uppercase; color: var(--gold); margin-bottom: 1rem;
        }
        .section-title {
            font-family: 'Playfair Display', serif;
            font-size: clamp(1.8rem, 4vw, 3rem); font-weight: 400;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 2.5rem; max-width: 1400px; margin: 0 auto;
        }
        .product-card {
            background: var(--dark-gray); border: 1px solid var(--medium-gray);
            overflow: hidden; transition: all 0.4s; position: relative;
        }
        .product-card:hover {
            transform: translateY(-8px); border-color: var(--gold);
        }
        .product-image {
            width: 100%; height: 350px; background: var(--medium-gray);
            display: flex; align-items: center; justify-content: center;
            position: relative; overflow: hidden;
        }
        .product-image img {
            width: 100%; height: 100%; object-fit: cover;
            transition: transform 0.6s;
        }
        .product-card:hover .product-image img { transform: scale(1.05); }
        .product-badge {
            position: absolute; top: 1rem; left: 1rem;
            background: var(--gold); color: var(--black);
            padding: 0.4rem 0.8rem; font-size: 0.65rem;
            letter-spacing: 2px; text-transform: uppercase; font-weight: 600;
        }
        .product-info { padding: 1.8rem; }
        .product-category {
            font-size: 0.7rem; letter-spacing: 3px;
            text-transform: uppercase; color: var(--gold); margin-bottom: 0.5rem;
        }
        .product-name {
            font-family: 'Playfair Display', serif;
            font-size: 1.3rem; margin-bottom: 0.8rem;
        }
        .product-description {
            font-size: 0.85rem; color: var(--light-gray);
            line-height: 1.6; margin-bottom: 1.2rem;
        }
        .product-footer {
            display: flex; justify-content: space-between; align-items: center;
            padding-top: 1.2rem; border-top: 1px solid var(--medium-gray);
        }
        .product-price {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem; color: var(--gold);
        }
        .product-price span { font-size: 0.9rem; color: var(--light-gray); }
        .add-cart-btn {
            padding: 0.7rem 1.5rem; background: transparent;
            border: 1px solid var(--gold); color: var(--gold);
            font-family: 'Inter', sans-serif; font-size: 0.75rem;
            letter-spacing: 2px; text-transform: uppercase;
            cursor: pointer; transition: all 0.3s;
        }
        .add-cart-btn:hover { background: var(--gold); color: var(--black); }
        .add-cart-btn.added { background: var(--success); border-color: var(--success); color: white; }

        .loading-state, .empty-state {
            text-align: center; padding: 4rem; color: var(--light-gray);
            grid-column: 1 / -1;
        }
        .spinner {
            width: 40px; height: 40px; border: 2px solid var(--medium-gray);
            border-top-color: var(--gold); border-radius: 50%;
            animation: spin 1s linear infinite; margin: 0 auto 1rem;
        }
        @keyframes spin { to { transform: rotate(360deg); } }

        .modal-overlay {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.8); z-index: 3000;
            display: none; align-items: center; justify-content: center;
            backdrop-filter: blur(5px);
        }
        .modal-overlay.open { display: flex; }
        .modal {
            background: var(--dark-gray); border: 1px solid var(--medium-gray);
            width: 90%; max-width: 600px; max-height: 80vh;
            overflow-y: auto; position: relative;
        }
        .modal-header {
            padding: 1.5rem; border-bottom: 1px solid var(--medium-gray);
            display: flex; justify-content: space-between; align-items: center;
        }
        .modal-header h2 {
            font-family: 'Playfair Display', serif; font-size: 1.5rem;
        }
        .modal-close {
            background: none; border: none; color: var(--light-gray);
            font-size: 1.5rem; cursor: pointer;
        }
        .modal-close:hover { color: var(--gold); }
        .modal-body { padding: 1.5rem; }
        .cart-item {
            display: flex; gap: 1rem; padding: 1rem 0;
            border-bottom: 1px solid var(--medium-gray);
        }
        .cart-item-img {
            width: 80px; height: 80px; background: var(--medium-gray);
            display: flex; align-items: center; justify-content: center;
            font-size: 2rem; flex-shrink: 0;
        }
        .cart-item-img img { width: 100%; height: 100%; object-fit: cover; }
        .cart-item-info { flex: 1; }
        .cart-item-name { font-family: 'Playfair Display', serif; margin-bottom: 0.3rem; }
        .cart-item-price { color: var(--gold); font-size: 0.9rem; }
        .cart-item-qty {
            display: flex; align-items: center; gap: 0.8rem; margin-top: 0.5rem;
        }
        .qty-btn {
            width: 28px; height: 28px; background: var(--black);
            border: 1px solid var(--medium-gray); color: var(--white);
            cursor: pointer; font-size: 1rem; display: flex;
            align-items: center; justify-content: center;
        }
        .qty-btn:hover { border-color: var(--gold); }
        .remove-btn {
            background: none; border: none; color: #c44;
            cursor: pointer; font-size: 0.75rem; margin-top: 0.5rem;
        }
        .cart-total {
            padding: 1.5rem; border-top: 1px solid var(--medium-gray);
            display: flex; justify-content: space-between; align-items: center;
        }
        .cart-total-amount {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem; color: var(--gold);
        }
        .checkout-btn {
            padding: 1rem 2rem; background: var(--gold); border: none;
            color: var(--black); font-family: 'Inter', sans-serif;
            font-size: 0.85rem; letter-spacing: 2px; text-transform: uppercase;
            cursor: pointer; font-weight: 600; transition: all 0.3s;
        }
        .checkout-btn:hover { background: var(--gold-light); }
        .checkout-btn:disabled { opacity: 0.5; cursor: not-allowed; }

        .checkout-form { padding: 1.5rem; }
        .form-group { margin-bottom: 1.2rem; }
        .form-group label {
            display: block; font-size: 0.75rem;
            letter-spacing: 2px; text-transform: uppercase;
            color: var(--light-gray); margin-bottom: 0.5rem;
        }
        .form-group input, .form-group textarea, .form-group select {
            width: 100%; padding: 0.9rem; background: var(--black);
            border: 1px solid var(--medium-gray); color: var(--white);
            font-family: 'Inter', sans-serif; font-size: 0.9rem;
        }
        .form-group input:focus, .form-group textarea:focus, .form-group select:focus {
            outline: none; border-color: var(--gold);
        }
        .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
        .submit-order-btn {
            width: 100%; padding: 1.2rem; background: var(--gold); border: none;
            color: var(--black); font-family: 'Inter', sans-serif;
            font-size: 0.9rem; letter-spacing: 3px; text-transform: uppercase;
            cursor: pointer; font-weight: 600; margin-top: 1rem;
        }
        .submit-order-btn:hover { background: var(--gold-light); }

        .success-msg {
            text-align: center; padding: 3rem 2rem;
        }
        .success-msg h3 {
            font-family: 'Playfair Display', serif; font-size: 1.8rem;
            color: var(--gold); margin-bottom: 1rem;
        }
        .success-msg p { color: var(--light-gray); line-height: 1.7; }
        .success-icon { font-size: 4rem; margin-bottom: 1rem; }

        .info-section {
            padding: 6rem 3rem; background: var(--dark-gray);
        }
        .info-grid {
            display: grid; grid-template-columns: repeat(3, 1fr);
            gap: 3rem; max-width: 1200px; margin: 0 auto;
        }
        .info-card {
            text-align: center; padding: 2.5rem 1.5rem;
            border: 1px solid var(--medium-gray); transition: all 0.3s;
        }
        .info-card:hover { border-color: var(--gold); transform: translateY(-5px); }
        .info-icon { font-size: 2.2rem; margin-bottom: 1.2rem; color: var(--gold); }
        .info-title { font-family: 'Playfair Display', serif; font-size: 1.2rem; margin-bottom: 0.8rem; }
        .info-text { font-size: 0.85rem; color: var(--light-gray); line-height: 1.7; }

        footer {
            padding: 3rem; text-align: center;
            border-top: 1px solid var(--medium-gray);
        }
        .footer-logo {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem; color: var(--gold); margin-bottom: 1rem;
        }
        .footer-text { font-size: 0.85rem; color: var(--light-gray); margin-bottom: 1.5rem; }
        .footer-links { display: flex; justify-content: center; gap: 2rem; margin-bottom: 1.5rem; }
        .footer-links a {
            color: var(--light-gray); text-decoration: none;
            font-size: 0.75rem; letter-spacing: 2px; text-transform: uppercase;
        }
        .footer-links a:hover { color: var(--gold); }
        .copyright { font-size: 0.75rem; color: var(--light-gray); }

        @media (max-width: 768px) {
            nav { padding: 1rem 1.5rem; }
            .products-section, .info-section { padding: 4rem 1.5rem; }
            .products-grid { grid-template-columns: 1fr; }
            .info-grid { grid-template-columns: 1fr; }
            .form-row { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <div class="video-container">
        <video autoplay muted loop playsinline>
            <source src="https://assets.mixkit.co/videos/preview/mixkit-luxury-clothing-store-interior-4747-large.mp4" type="video/mp4">
        </video>
    </div>
    <div class="video-overlay"></div>

    <nav>
        <div class="logo" id="shopName">BOUTIQUE</div>
        <div class="nav-right">
            <button class="cart-btn" id="cartBtn">
                🛒 Cart <span class="cart-count" id="cartCount" style="display:none">0</span>
            </button>
        </div>
    </nav>

    <section class="hero" id="home">
        <p class="hero-subtitle" id="heroSubtitle">Premium Store</p>
        <h1 class="hero-title">
            <span id="heroTitle1">Discover</span><br>
            <em id="heroTitle2">Elegance</em>
        </h1>
        <p class="hero-desc" id="heroDesc">Curated luxury products delivered to your doorstep.</p>
        <a href="#collection" class="cta-button">Shop Now</a>
    </section>

    <section class="products-section" id="collection">
        <div class="section-header">
            <p class="section-label">Collection</p>
            <h2 class="section-title" id="collectionTitle">Our Products</h2>
        </div>
        <div class="products-grid" id="productsGrid">
            <div class="loading-state">
                <div class="spinner"></div>
                <p>Loading products...</p>
            </div>
        </div>
    </section>

    <section class="info-section" id="about">
        <div class="section-header">
            <p class="section-label">Why Us</p>
            <h2 class="section-title">Our Promise</h2>
        </div>
        <div class="info-grid">
            <div class="info-card">
                <div class="info-icon">🚚</div>
                <h3 class="info-title">Fast Delivery</h3>
                <p class="info-text" id="deliveryText">Nationwide delivery with competitive rates.</p>
            </div>
            <div class="info-card">
                <div class="info-icon">✨</div>
                <h3 class="info-title">Premium Quality</h3>
                <p class="info-text" id="qualityText">Carefully selected products for our customers.</p>
            </div>
            <div class="info-card">
                <div class="info-icon">🤝</div>
                <h3 class="info-title">Trusted Service</h3>
                <p class="info-text" id="serviceText">Cash on delivery and easy returns.</p>
            </div>
        </div>
    </section>

    <footer>
        <div class="footer-logo" id="footerLogo">BOUTIQUE</div>
        <p class="footer-text" id="footerText">Your trusted shop.</p>
        <div class="footer-links">
            <a href="#">Instagram</a>
            <a href="#">Facebook</a>
            <a href="#">WhatsApp</a>
        </div>
        <p class="copyright" id="copyright">© 2024 All rights reserved.</p>
    </footer>

    <div class="modal-overlay" id="cartModal">
        <div class="modal">
            <div class="modal-header">
                <h2>Your Cart</h2>
                <button class="modal-close" id="closeCart">✕</button>
            </div>
            <div class="modal-body" id="cartBody">
                <div id="cartItems"></div>
                <div class="cart-total" id="cartTotal" style="display:none">
                    <span>Total:</span>
                    <span class="cart-total-amount" id="totalAmount">0 DZD</span>
                </div>
                <button class="checkout-btn" id="checkoutBtn" style="display:none">Proceed to Checkout</button>
            </div>
        </div>
    </div>

    <div class="modal-overlay" id="checkoutModal">
        <div class="modal">
            <div class="modal-header">
                <h2>Complete Your Order</h2>
                <button class="modal-close" id="closeCheckout">✕</button>
            </div>
            <div class="modal-body">
                <form class="checkout-form" id="orderForm">
                    <div class="form-row">
                        <div class="form-group">
                            <label>Full Name *</label>
                            <input type="text" id="custName" required placeholder="Your name">
                        </div>
                        <div class="form-group">
                            <label>Phone *</label>
                            <input type="tel" id="custPhone" required placeholder="+213...">
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Email</label>
                        <input type="email" id="custEmail" placeholder="email@example.com">
                    </div>
                    <div class="form-group">
                        <label>Wilaya *</label>
                        <select id="custWilaya" required>
                            <option value="">Select your wilaya</option>
                            <option value="Alger">Alger</option>
                            <option value="Oran">Oran</option>
                            <option value="Constantine">Constantine</option>
                            <option value="Annaba">Annaba</option>
                            <option value="Blida">Blida</option>
                            <option value="Other">Other</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>Full Address *</label>
                        <textarea id="custAddress" rows="2" required placeholder="Street, building, floor..."></textarea>
                    </div>
                    <div class="form-group">
                        <label>Notes (optional)</label>
                        <textarea id="custNotes" rows="2" placeholder="Any special requests..."></textarea>
                    </div>
                    <div style="background: var(--black); padding: 1rem; margin-bottom: 1rem;">
                        <p style="font-size: 0.8rem; color: var(--light-gray); margin-bottom: 0.5rem;">Order Summary:</p>
                        <p id="orderSummary" style="font-family: 'Playfair Display', serif; color: var(--gold); font-size: 1.2rem;"></p>
                    </div>
                    <button type="submit" class="submit-order-btn">Place Order</button>
                </form>
                <div class="success-msg" id="orderSuccess" style="display:none">
                    <div class="success-icon">✅</div>
                    <h3>Order Placed!</h3>
                    <p>Thank you for your order. We will contact you shortly to confirm delivery details.</p>
                    <p style="margin-top: 1rem; font-size: 0.8rem; color: var(--light-gray);">Order ID: <span id="orderId"></span></p>
                </div>
            </div>
        </div>
    </div>

    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";
        import { getFirestore, collection, getDocs, addDoc, query, orderBy } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";

        const firebaseConfig = {
            apiKey: "AIzaSyDtzlI-Hvftx0fRgwmTBSljQWRGrG63oTo",
            authDomain: "nabil-5503c.firebaseapp.com",
            projectId: "nabil-5503c",
            storageBucket: "nabil-5503c.firebasestorage.app",
            messagingSenderId: "1072196412989",
            appId: "1:1072196412989:web:e04ac19fc4870db52f6c54"
        };

        const app = initializeApp(firebaseConfig);
        const db = getFirestore(app);

        let cart = [];

        async function loadProducts() {
            const grid = document.getElementById('productsGrid');
            try {
                const q = query(collection(db, 'products'), orderBy('createdAt', 'desc'));
                const snapshot = await getDocs(q);
                
                if (snapshot.empty) {
                    grid.innerHTML = `
                        <div class="empty-state">
                            <div style="font-size: 3rem; margin-bottom: 1rem; opacity: 0.5;">📦</div>
                            <p>No products available yet. Check back soon!</p>
                        </div>
                    `;
                    return;
                }

                grid.innerHTML = '';
                snapshot.forEach(doc => {
                    const p = doc.data();
                    const card = document.createElement('div');
                    card.className = 'product-card';
                    card.innerHTML = `
                        <div class="product-image">
                            ${p.image ? `<img src="${p.image}" alt="${p.name}" onerror="this.style.display='none'; this.parentElement.innerHTML='<span style=\\"font-size:3rem\\">🛍️</span>'">` : '<span style="font-size:3rem">🛍️</span>'}
                            ${p.badge ? `<div class="product-badge">${p.badge}</div>` : ''}
                        </div>
                        <div class="product-info">
                            <p class="product-category">${p.category || 'Product'}</p>
                            <h3 class="product-name">${p.name}</h3>
                            <p class="product-description">${p.description || ''}</p>
                            <div class="product-footer">
                                <div class="product-price">${parseInt(p.price).toLocaleString()} <span>DZD</span></div>
                                <button class="add-cart-btn" onclick="addToCart('${doc.id}', '${p.name}', ${p.price}, '${p.image || ''}')">Add to Cart</button>
                            </div>
                        </div>
                    `;
                    grid.appendChild(card);
                });
            } catch (err) {
                grid.innerHTML = `
                    <div class="empty-state">
                        <p style="color: #c44;">⚠️ Please connect Firebase to see products.</p>
                        <p style="font-size: 0.8rem; margin-top: 0.5rem;">Open the code and add your Firebase config.</p>
                    </div>
                `;
            }
        }

        window.addToCart = function(id, name, price, image) {
            const existing = cart.find(item => item.id === id);
            if (existing) {
                existing.qty++;
            } else {
                cart.push({ id, name, price, image, qty: 1 });
            }
            updateCartUI();
            event.target.textContent = '✓ Added';
            event.target.classList.add('added');
            setTimeout(() => {
                event.target.textContent = 'Add to Cart';
                event.target.classList.remove('added');
            }, 1500);
        };

        function updateCartUI() {
            const count = cart.reduce((sum, item) => sum + item.qty, 0);
            const badge = document.getElementById('cartCount');
            if (count > 0) {
                badge.textContent = count;
                badge.style.display = 'flex';
            } else {
                badge.style.display = 'none';
            }
            renderCartItems();
        }

        function renderCartItems() {
            const container = document.getElementById('cartItems');
            const totalEl = document.getElementById('cartTotal');
            const checkoutBtn = document.getElementById('checkoutBtn');
            
            if (cart.length === 0) {
                container.innerHTML = '<p style="text-align:center; color: var(--light-gray); padding: 2rem;">Your cart is empty</p>';
                totalEl.style.display = 'none';
                checkoutBtn.style.display = 'none';
                return;
            }

            let html = '';
            let total = 0;
            cart.forEach((item, idx) => {
                total += item.price * item.qty;
                html += `
                    <div class="cart-item">
                        <div class="cart-item-img">
                            ${item.image ? `<img src="${item.image}" alt="">` : '🛍️'}
                        </div>
                        <div class="cart-item-info">
                            <div class="cart-item-name">${item.name}</div>
                            <div class="cart-item-price">${item.price.toLocaleString()} DZD each</div>
                            <div class="cart-item-qty">
                                <button class="qty-btn" onclick="changeQty(${idx}, -1)">−</button>
                                <span>${item.qty}</span>
                                <button class="qty-btn" onclick="changeQty(${idx}, 1)">+</button>
                            </div>
                            <button class="remove-btn" onclick="removeFromCart(${idx})">Remove</button>
                        </div>
                    </div>
                `;
            });
            container.innerHTML = html;
            document.getElementById('totalAmount').textContent = total.toLocaleString() + ' DZD';
            totalEl.style.display = 'flex';
            checkoutBtn.style.display = 'block';
        }

        window.changeQty = function(idx, delta) {
            cart[idx].qty += delta;
            if (cart[idx].qty <= 0) cart.splice(idx, 1);
            updateCartUI();
        };

        window.removeFromCart = function(idx) {
            cart.splice(idx, 1);
            updateCartUI();
        };

        document.getElementById('cartBtn').addEventListener('click', () => {
            document.getElementById('cartModal').classList.add('open');
        });
        document.getElementById('closeCart').addEventListener('click', () => {
            document.getElementById('cartModal').classList.remove('open');
        });
        document.getElementById('checkoutBtn').addEventListener('click', () => {
            document.getElementById('cartModal').classList.remove('open');
            document.getElementById('checkoutModal').classList.add('open');
            const total = cart.reduce((sum, item) => sum + item.price * item.qty, 0);
            document.getElementById('orderSummary').textContent = 
                cart.length + ' item(s) — ' + total.toLocaleString() + ' DZD';
        });
        document.getElementById('closeCheckout').addEventListener('click', () => {
            document.getElementById('checkoutModal').classList.remove('open');
        });
        document.querySelectorAll('.modal-overlay').forEach(overlay => {
            overlay.addEventListener('click', (e) => {
                if (e.target === overlay) overlay.classList.remove('open');
            });
        });

        document.getElementById('orderForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const btn = e.target.querySelector('.submit-order-btn');
            btn.textContent = 'Processing...';
            btn.disabled = true;

            const orderData = {
                customer: {
                    name: document.getElementById('custName').value,
                    phone: document.getElementById('custPhone').value,
                    email: document.getElementById('custEmail').value,
                    wilaya: document.getElementById('custWilaya').value,
                    address: document.getElementById('custAddress').value,
                    notes: document.getElementById('custNotes').value
                },
                items: cart,
                total: cart.reduce((sum, item) => sum + item.price * item.qty, 0),
                status: 'new',
                createdAt: new Date().toISOString()
            };

            try {
                const docRef = await addDoc(collection(db, 'orders'), orderData);
                document.getElementById('orderForm').style.display = 'none';
                document.getElementById('orderSuccess').style.display = 'block';
                document.getElementById('orderId').textContent = docRef.id;
                cart = [];
                updateCartUI();
            } catch (err) {
                alert('Error placing order. Please check your Firebase config.');
                btn.textContent = 'Place Order';
                btn.disabled = false;
            }
        });

        const cfg = window.siteConfig || {};
        if (cfg.shopName) {
            document.getElementById('shopName').textContent = cfg.shopName;
            document.getElementById('footerLogo').textContent = cfg.shopName;
            document.getElementById('pageTitle').textContent = cfg.shopName;
        }
        if (cfg.subtitle) document.getElementById('heroSubtitle').textContent = cfg.subtitle;
        if (cfg.heroTitle1) document.getElementById('heroTitle1').textContent = cfg.heroTitle1;
        if (cfg.heroTitle2) document.getElementById('heroTitle2').textContent = cfg.heroTitle2;
        if (cfg.heroDesc) document.getElementById('heroDesc').textContent = cfg.heroDesc;
        if (cfg.collectionTitle) document.getElementById('collectionTitle').textContent = cfg.collectionTitle;
        if (cfg.delivery) document.getElementById('deliveryText').textContent = cfg.delivery;
        if (cfg.quality) document.getElementById('qualityText').textContent = cfg.quality;
        if (cfg.service) document.getElementById('serviceText').textContent = cfg.service;
        if (cfg.footer) document.getElementById('footerText').textContent = cfg.footer;
        if (cfg.contact) document.getElementById('copyright').innerHTML = '© 2024 ' + cfg.shopName + ' | ' + cfg.contact;

        loadProducts();
    </script>
    <script src="config.js"></script>
</body>
</html>